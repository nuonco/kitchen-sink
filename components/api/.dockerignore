# Keep local build output out of the build context. The Dockerfile also wipes
# the Go module and build caches, because kaniko builds on the runner have been
# seen reusing a polluted filesystem -- see the comment at the top of it.
**/.DS_Store
/api
/worker
