# Build output and installed dependencies. dist/ is generated by `npm run build`
# and embedded into the Go binary at compile time, so it must exist locally
# before `go build` -- but it is never committed.
node_modules/
dist/
