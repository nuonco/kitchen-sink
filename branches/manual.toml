# Never runs automatically; an operator selects and submits a commit.
name = "manual-release"
post_deploy_runbooks = ["full-health-check"]

[public_repo]
repo      = "nuonco/kitchen-sink"
directory = "."
branch    = "jm/change-tests"

[run]
mode = "manual_only"

[[install_groups]]
name  = "staging"
order = 1

[install_groups.label_selector]
env = "staging"
