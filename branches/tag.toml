# branch
# this branch is updated when a git tag is pushed with the provided prefix
name = "tag-release"

[public_repo]
repo      = "nuonco/kitchen-sink"
directory = "."
branch    = "jm/app-branch-tests-20260917"

[run]
mode = "on_tag"
tag_prefix = "app-branch-release/"

[[install_groups]]
name  = "default"
order = 1

[install_groups.label_selector]
env = "default"
