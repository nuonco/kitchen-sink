version = "v2"

display_name = "Relay"
description  = "Self-hosted webhook delivery: an ingest API, a Postgres-backed queue, and a delivery engine with retries, backoff, and a dead-letter queue — deployed into your own account."
readme       = "./control-plane.md"
