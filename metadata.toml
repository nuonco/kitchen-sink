version = "v2"

display_name = "Conduit"
description  = "Self-hosted data sync: pipelines run in your account and data never leaves it. This app config ships the sync engine, its destination bucket, and the full operational surface around them."
readme       = "./control-plane.md"
