# Policies

For an AWS application, this policies directory holds _.json files and _.toml files.

the `Toml` files define named policies. the `json` files may be used in named policies or in inline policies.
