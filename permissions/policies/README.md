# Policies

For an AWS application, this policies directory holds `json` files and `toml` files. The `toml` files define named
policies. The `json` files may be used in named policies or in inline policies.
